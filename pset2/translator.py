# implement a "simple" version of Google translate, translating between only Engnlish and Spanish, supporting the following operations:
# 1. Adding translation 
# 2. Getting translations

class Translator:  #create new dictionaries for each language ? 
    
    # hello -> hola for english to spanish vice versa for spanish to english
    # use api calls to get the database ?for each language
    
    def __init__ ():
        english_to_spanish = {}
        spanish_to_english = {}
        
    # Returns nothing, adds a translation
    def add_translation(dict, key, value) -> bool:
        return None
        
        
        
    def get_translation():
        return
    
    
    